package main

func main() {
	/* memory allocation and deallocation happens automatically
		 two methods: New() Make()
		 New()- Allocate memory but not initialised
	              we get a memory address
				  zeroed storage (we cant place any data initially)

		 Make()- Allocate memory and initialised
			we get a memory address
			non-zeroed storage (we can place any data initially)

			*Garbage collection happens automatically
	*/
}
